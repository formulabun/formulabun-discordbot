# srb2kart discordbot, a discord bot for srb2kart servers

## features:
* show numbers of players in status
* list players
* generic help

## getting started
* clone this repository with `git clone`
* copy the `env.template` file to `.env`
* fill in the variables in `.env`
* run `npm install`
* run `npm run main`
